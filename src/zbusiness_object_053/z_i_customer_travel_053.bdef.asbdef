interface;
use draft;

define behavior for Z_I_CUSTOMER_TRAVEL_053 alias Z_CUSTOMER053
{
  use create;
  use update;
  use delete;

  use action acceptCustomer;
  use action rejectCustomer;

  use action Resume;
  use action Edit;
  use action Activate;
  use action Discard;
  use action Prepare;
}